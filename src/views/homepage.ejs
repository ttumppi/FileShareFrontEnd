<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="http://dabblestudios.tplinkdns.com:16000/css">
    <title>Homepage</title>
  </head>
  <body>

    <% if (files)%>
      <ul>
        <% foreach(file in files) %>
          <%file%>
        %>
      </ul>
    
  </body>
</html>